# Skills mounted into the gateway shards

A copy of `skills/plusgpt/` from the MCP server repository
(`github.com/shanaka95/knowldgebase-mcp`), which is where it is maintained and
tested against the live tool list.

It is copied here rather than authored here so there is one canonical version.
Refresh it with:

    cp -R ../knowledgebase-mcp/skills/plusgpt hermes-skills/
